<video poster='https://www.talltree.com.cn/exercises/img.png' src="{{highMath[sessionId].section[id].url}}"  bindended="shikan"
       controls autoplay enable-progress-gesture page-gesture></video>
<view class="intro">
    <view class="title">{{highMath[sessionId].section[id].math_name}}
        <block wx:if="{{isBuy[lessonId].lessonIsBuy}}">
          <button class="share" style="float:right">做题</button>
        </block>

        <block wx:else>
          <button class="share" style="float:right" bindtap="wxPay">购买</button>
        </block>
        
    </view>
    <view class="content">
        <text>主讲：数学老师</text>
        <text>|</text>
        <text>课时：{{highMath[lessonId].section.length}}</text>
        <text>|</text>
        <text>562人已学习</text>
    </view>
</view>
<view class="box">
    <view class="weui-tab">
        <view class="weui-navbar">

            <block wx:for="{{tabs}}" wx:key="*this">
                <view id="{{index}}" class="ff weui-navbar__item {{activeIndex == index ? 'weui-bar__item_on' : ''}}"
                      bindtap="tabClick">
                    <view class="weui-navbar__title ziti">{{item}}</view>
                </view>
            </block>


        </view>
        <view class="weui-tab__panel bao">
            <view class="weui-tab__content" hidden="{{activeIndex != 0}}">

                        <block wx:for="{{highMath}}" wx:for-index='sessionIdx' wx:for-item="parentItem">
                            <view>
                                <view class="chapter" catchtap="listTap" data-parentindex="{{sessionIdx}}">
                                    <text class="list_item_name" data-chapter="{{sessionIdx}}" bindtap='setTitle'>{{parentItem.sectionName}}</text>
                                    <image class='icon_down {{parentItem.show&&"icon_down_rotate"}}' src='../../images/icon_down.png'></image>
                                </view>



                                    <block wx:if="{{parentItem.show}}" wx:for="{{parentItem.section}}" wx:for-item="childItem" wx:key="this" catchTap="listItemTap"  data-parentindex='{{parentIndex}}' wx:for-index="idx">

                                
                                    
                                        <view data-idx="{{idx}}" data-sessionIdx="{{sessionIdx}}" class="cell {{(idx == id &&sessionIdx==sessionId)? 'colore' : 'a2'}}" bindtap='huan'>
                                            <view>{{childItem.math_name}}</view>
                                            <view>{{childItem.TD}}</view>
                                        </view>

                                


                            
                                        
                                    </block>


                            </view>

                        </block>
            </view>
            <view class="weui-tab__content" hidden="{{activeIndex != 2}}">
                <block wx:for="{{comData}}">
                    <view class="nei">
                        <view class="comcell">
                            <image src="{{item.lujing}}"></image>
                            <view class="r">
                                <view class="rtitle">{{item.title}}</view>
                                <view>{{item.subtitle}}</view>
                            </view>
                        </view>
                    </view>
                </block>
            </view>
            <view class="weui-tab__content" hidden="{{activeIndex != 1}}">
                <view class="xiang">
                    <text>这里有各种奇思妙想，</text>
                    <text>有疯狂的简笔画，</text>
                    <text>会教你用叉子打蝴蝶结，</text>
                    <text>各种新奇玩意，</text>
                    <text>只要你在生活中发现任何美好的事物，</text>
                    <text>我们都能动手制作出来。</text>
                    <text></text>
                    <text></text>
                    <text>陶艺体验、美味厨艺、创意手工、艺术涂鸦</text>
                    <text>等多种不同艺术形式，给孩子留下美好的童年记忆。</text>

                </view>
            </view>
        </view>
    </view>
</view>


